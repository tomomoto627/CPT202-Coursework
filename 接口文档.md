## 接口文档（前端视角整理）

**基础说明**

- **服务基础地址**: `VITE_API_BASE_URL`（由环境变量配置）
- **统一前缀**: 下文所有 URL 均为相对路径，需要拼接到基础地址后使用
- **认证方式**:
  - 登录后返回 `token`，前端存储在 `localStorage`：`booking.auth.token`
  - 后续请求通过请求头 `Authorization: Bearer <token>` 传递

---

## 一、认证 & 用户（Auth & User）

### - [Done] 1. 发送注册邮箱验证码

- **URL**: `/auth/send-email-code`
- **方法**: `POST`
- **请求头**:
  - `Content-Type: application/json`
  - 无需 `Authorization`（未注册/未登录状态使用）
- **请求体 `payload`**:


| 字段名   | 类型     | 必填  | 说明                                |
| ----- | ------ | --- | --------------------------------- |
| email | string | 是   | 注册使用的邮箱地址                         |
| scene | string | 否   | 场景标记，前端传 `"register"` 或 `"login"` |


- **示例请求体**:

```json
{
  "email": "user@example.com",
  "scene": "register"
}
```

- **响应（建议格式）**:

```json
{
  "code": "OK",
  "message": "验证码已发送"
}
```

---

### - [Done] 2. 注册（邮箱+验证码）

- **URL**: `/auth/register`
- **方法**: `POST`
- **请求头**:
  - `Content-Type: application/json`
  - 无需 `Authorization`
- **请求体 `payload`**:


| 字段名              | 类型     | 必填  | 说明                |
| ---------------- | ------ | --- | ----------------- |
| name             | string | 是   | 用户昵称/姓名           |
| email            | string | 是   | 注册邮箱              |
| password         | string | 是   | 登录密码（明文或加密，视后端实现） |
| verificationCode | string | 是   | 邮箱收到的验证码（如 6 位数字） |


- **示例请求体**:

```json
{
  "name": "张三",
  "email": "user@example.com",
  "password": "password123",
  "verificationCode": "123456"
}
```

- **成功响应（前端期望）**:

```json
{
  "token": "jwt-or-other-token",
  "user": {
    "id": "user-id",
    "name": "张三",
    "email": "user@example.com",
    "role": "Customer"
  }
}
```

---

### - [Done] 3. 登录

- **URL**: `/auth/login`
- **方法**: `POST`
- **请求体 `payload`**:


| 字段名      | 类型     | 必填  | 说明   |
| -------- | ------ | --- | ---- |
| email    | string | 是   | 登录邮箱 |
| password | string | 是   | 登录密码 |


- **响应**（与注册成功一致）:

```json
{
  "token": "jwt-or-other-token",
  "user": {
    "id": "user-id",
    "name": "张三",
    "email": "user@example.com",
    "role": "Customer"
  }
}
```

---

### - [Done] 3.1 邮箱验证码登录

- **URL**: `/auth/login-by-email-code`
- **方法**: `POST`
- **请求体 `payload`**:


| 字段名              | 类型     | 必填  | 说明            |
| ---------------- | ------ | --- | ------------- |
| email            | string | 是   | 登录邮箱          |
| verificationCode | string | 是   | 邮箱验证码（通常 6 位） |


- **示例请求体**:

```json
{
  "email": "user@example.com",
  "verificationCode": "123456"
}
```

- **响应**（与密码登录一致）:

```json
{
  "token": "jwt-or-other-token",
  "user": {
    "id": "user-id",
    "name": "张三",
    "email": "user@example.com",
    "role": "Customer"
  }
}
```

---

### - [Done] 4. 登出

- **URL**: `/auth/logout`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`
- **请求体**: 无
- **响应（示例）**:

```json
{
  "code": "OK",
  "message": "已登出"
}
```

---

### - [Done] 5. 获取当前用户信息

- **URL**: `/me`
- **方法**: `GET`
- **请求头**: 需要 `Authorization`
- **请求参数**: 无
- **响应（推荐格式）**:

```json
{
  "user": {
    "id": "user-id",
    "name": "张三",
    "email": "user@example.com",
    "role": "Customer"
  }
}
```

---

### - [Done] 6. 更新当前用户信息

- **URL**: `/me`
- **方法**: `PATCH`
- **请求头**: 需要 `Authorization`
- **请求体 `payload`**（部分更新即可）:


| 字段名    | 类型     | 必填  | 说明     |
| ------ | ------ | --- | ------ |
| name   | string | 否   | 修改后的姓名 |
| avatar | string | 否   | 头像 URL |
| 其他     | any    | 否   | 视业务扩展  |


- **响应（建议）**:

```json
{
  "user": {
    "id": "user-id",
    "name": "新名字",
    "email": "user@example.com",
    "role": "Customer",
    "avatar": "https://..."
  }
}
```

---

## 二、专长 & 专家（Expertise & Specialists）

### - [Done] 1. 获取专长列表

- **URL**: `/expertise`
- **方法**: `GET`
- **请求参数**: 无
- **响应示例**:

```json
[
  {
    "id": "exp-1",
    "name": "职业发展",
    "description": "职业规划与咨询"
  },
  {
    "id": "exp-2",
    "name": "情绪压力",
    "description": "情绪管理与减压"
  }
]
```

---

### - [Done] 2. 获取专家列表

- **URL**: `/specialists`
- **方法**: `GET`
- **查询参数 `params`**（可选）:


| 字段名         | 类型     | 说明               |
| ----------- | ------ | ---------------- |
| expertiseId | string | 过滤：按专长 ID        |
| page        | number | 页码，从 1 开始        |
| pageSize    | number | 每页条数             |
| 其他筛选项       | any    | 如地域、价格区间等，可按业务扩展 |


- **响应示例**:

```json
{
  "items": [
    {
      "id": "sp-1",
      "name": "李老师",
      "expertiseIds": ["exp-1", "exp-2"],
      "price": 300
    }
  ],
  "total": 1,
  "page": 1,
  "pageSize": 10
}
```

---

### - [Done] 3. 获取专家详情

- **URL**: `/specialists/{id}`
- **方法**: `GET`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 专家ID |


- **响应示例**:

```json
{
  "id": "sp-1",
  "name": "李老师",
  "bio": "多年咨询经验...",
  "expertise": [{ "id": "exp-1", "name": "职业发展" }],
  "price": 300
}
```

---

### - [Done] 4. 获取专家可预约时段

- **URL**: `/specialists/{id}/slots`
- **方法**: `GET`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 专家ID |


- **查询参数 `params`**（建议）:


| 字段名  | 类型     | 说明                    |
| ---- | ------ | --------------------- |
| date | string | 查询日期，如 `"2026-03-17"` |
| from | string | 起始时间，可选               |
| to   | string | 结束时间，可选               |


- **响应示例**:

```json
[
  {
    "slotId": "slot-1",
    "start": "2026-03-17T09:00:00+08:00",
    "end": "2026-03-17T09:30:00+08:00",
    "available": true,
    "amount": 120.00,
    "currency": "CNY",
    "duration": 30,
    "type": "online",
    "detail": "首次沟通，聚焦职业方向评估"
  }
]
```

- **字段补充说明（客户预约与改期页展示）**:
  - `amount` + `currency`: 时段费用
  - `duration`: 时段时长（分钟）
  - `type`: 咨询类型（如 `online` / `offline`）
  - `detail`: 时段补充说明

---

## 三、预约相关（Bookings - 客户）

### - [Done] 1. 创建预约

- **URL**: `/bookings`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`
- **请求体 `payload`**:


| 字段名          | 类型     | 必填  | 说明       |
| ------------ | ------ | --- | -------- |
| specialistId | string | 是   | 专家 ID    |
| slotId       | string | 是   | 选中的时段 ID |
| paymentId    | string | 是   | 支付单 ID（由支付接口返回） |
| note         | string | 否   | 备注/说明    |


- **响应示例**:

```json
{
  "id": "bk-1",
  "specialistId": "sp-1",
  "slotId": "slot-1",
  "status": "Pending"
}
```

---

### - [Done] 2. 获取我的预约列表

- **URL**: `/bookings`
- **方法**: `GET`
- **请求头**: 需要 `Authorization`
- **查询参数 `params`**（可选）:


| 字段名      | 类型     | 说明                             |
| -------- | ------ | ------------------------------ |
| status   | string | 筛选状态，如 `Pending/Confirmed/...` |
| page     | number | 页码                             |
| pageSize | number | 每页条数                           |
| from     | string | 开始日期                           |
| to       | string | 结束日期                           |


- **响应示例**:

```json
{
  "items": [
    {
      "id": "bk-1",
      "specialistId": "sp-1",
      "specialistName": "Dr. Smith",
      "time": "2026-03-17T09:00:00+08:00",
      "status": "Pending"
    }
  ],
  "total": 1,
  "page": 1,
  "pageSize": 10
}
```

- **字段说明（新增/展示相关）**:
  - `specialistName`: 专家姓名，前端列表页优先展示该字段
  - `specialistId`: 仍保留用于兼容或回退展示

---

### - [Done] 3. 获取单个预约详情

- **URL**: `/bookings/{id}`
- **方法**: `GET`
- **请求头**: 需要 `Authorization`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 预约ID |


- **响应示例**:

```json
{
  "id": "bk-1",
  "customerId": "user-1",
  "specialistId": "sp-1",
  "specialistName": "Dr. Smith",
  "time": "2026-03-17T09:00:00+08:00",
  "status": "Confirmed",
  "note": "希望聊职业规划",
  "duration": "60 minutes",
  "amount": 120.00,
  "currency": "CNY",
  "type": "online",
  "detail": "首次沟通，聚焦职业方向评估"
}
```

- **字段变更说明**:
  - `duration` 已用于替代旧的 `slot` 展示含义（时长信息）
  - `specialistName` 用于页面展示专家名称
  - `specialistId` 仍保留作为标识字段（前端可作为回退值）
  - `amount` / `currency` / `type` / `detail` 用于 `Booking Details` 页展示该预约时段的费用与服务信息

---

### - [Done] 4. 取消预约

- **URL**: `/bookings/{id}/cancel`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 预约ID |


- **请求体 `payload`**（可选）:


| 字段名    | 类型     | 必填  | 说明   |
| ------ | ------ | --- | ---- |
| reason | string | 否   | 取消原因 |


- **响应示例**:

```json
{
  "id": "bk-1",
  "status": "Cancelled"
}
```

---

### - [Done] 5. 改期预约

- **URL**: `/bookings/{id}/reschedule`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 预约ID |

- **推荐前端交互流程（与当前页面实现一致）**:
  - 在 `Booking Details` 页面点击 **Reschedule** 按钮
  - 跳转到该专家的可用时段页面（复用 `/customer/specialists/{specialistId}/slots`）
  - 用户可在时段列表查看 `amount/currency/duration/type/detail`，选择新的 `slotId` 后提交本接口完成改期


- **请求体 `payload`**（示例字段）:


| 字段名    | 类型     | 必填  | 说明      |
| ------ | ------ | --- | ------- |
| slotId | string | 是   | 新的时段 ID |


- **响应示例**:

```json
{
  "id": "bk-1",
  "status": "Rescheduled",
  "slotId": "slot-2"
}
```

---

## 四、专家端工作流（Specialist workflow）

### - [Done] 1. 获取预约请求列表（专家端）

- **URL**: `/specialist/booking-requests`
- **方法**: `GET`
- **请求头**: 需要 `Authorization`（专家角色）
- **查询参数 `params`**（可选）:


| 字段名      | 类型     | 说明            |
| -------- | ------ | ------------- |
| status   | string | 如 `Pending` 等 |
| page     | number | 页码            |
| pageSize | number | 每页条数          |


- **响应示例**:

```json
{
  "items": [
    {
      "id": "bk-1",
      "customerName": "张三",
      "time": "2026-03-17T09:00:00+08:00",
      "status": "Pending"
    }
  ],
  "total": 1,
  "page": 1,
  "pageSize": 10
}
```

---

### - [Done] 2. 确认预约

- **URL**: `/specialist/bookings/{id}/confirm`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 预约ID |


- **请求体**: 无
- **响应示例**:

```json
{
  "id": "bk-1",
  "status": "Confirmed"
}
```

---

### - [Done] 3. 拒绝预约

- **URL**: `/specialist/bookings/{id}/reject`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 预约ID |


- **请求体 `payload`**（建议）:


| 字段名    | 类型     | 必填  | 说明   |
| ------ | ------ | --- | ---- |
| reason | string | 否   | 拒绝原因 |


- **响应示例**:

```json
{
  "id": "bk-1",
  "status": "Rejected"
}
```

---

### - [Done] 4. 完成预约

- **URL**: `/specialist/bookings/{id}/complete`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 预约ID |


- **请求体**: 可为空或包含会话总结等字段（视业务需要）
- **响应示例**:

```json
{
  "id": "bk-1",
  "status": "Completed"
}
```

---

### - [] 5. 专家管理自己的时段

#### 5.1 获取自己的时段列表

- **URL**: `/specialist/slots`
- **方法**: `GET`
- **请求头**: 需要 `Authorization`（专家角色）
- **查询参数 `params`**（可选）:


| 字段名     | 类型      | 说明                   |
| ------ | ------- | -------------------- |
| date   | string  | 日期筛选（如 `2026-03-17`） |
| from   | string  | 起始时间筛选（可选）           |
| to     | string  | 结束时间筛选（可选）           |
| available | boolean | 是否仅返回可用时段（可选）        |


- **响应**: 返回时段对象列表。
- **响应示例**:

```json
[
  {
    "id": "slot-1",
    "specialistId": "sp-1",
    "date": "2026-03-17",
    "start": "09:00",
    "end": "09:30",
    "available": true,
    "amount": 120.00,
    "currency": "CNY",
    "duration": 30,
    "type": "online",
    "detail": "首次沟通，聚焦职业方向评估"
  }
]
```

---

#### 5.2 创建时段

- **URL**: `/specialist/slots`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`（专家角色）
- **请求体 `payload`**:


| 字段名     | 类型      | 必填  | 说明                       |
| ------ | ------- | --- | ------------------------ |
| date   | string  | 是   | 日期（如 `2026-03-17`）       |
| start  | string  | 是   | 开始时间（如 `09:00` 或 ISO 时间） |
| end    | string  | 是   | 结束时间（如 `09:30` 或 ISO 时间） |
| available | boolean | 否   | 是否可预约，默认 `true`          |
| amount | number  | 否   | 价格金额，默认 `0.00`           |
| currency | string  | 否   | 货币代码，默认 `CNY`            |
| duration | number  | 是   | 时长（分钟），必须大于 0            |
| type   | string  | 否   | 咨询类型，默认 `online`         |
| detail | string  | 否   | 补充说明（文本）                 |


- **响应**: 返回创建后的时段对象。
- **响应示例**:

```json
{
  "id": "slot-9",
  "specialistId": "sp-1",
  "date": "2026-03-18",
  "start": "14:00",
  "end": "14:30",
  "available": true,
  "amount": 120.00,
  "currency": "CNY",
  "duration": 30,
  "type": "online",
  "detail": "首次沟通，聚焦职业方向评估"
}
```

---

#### 5.3 获取单个时段详情

- **URL**: `/specialist/slots/{id}`
- **方法**: `GET`
- **请求头**: 需要 `Authorization`（专家角色）
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 时段ID |


- **响应**: 返回单个时段详情对象。
- **响应示例**:

```json
{
  "id": "slot-1",
  "specialistId": "sp-1",
  "date": "2026-03-17",
  "start": "09:00",
  "end": "09:30",
  "available": true,
  "amount": 120.00,
  "currency": "CNY",
  "duration": 30,
  "type": "online",
  "detail": "首次沟通，聚焦职业方向评估"
}
```

---

#### 5.4 更新时段

- **URL**: `/specialist/slots/{id}`
- **方法**: `PATCH`
- **请求头**: 需要 `Authorization`（专家角色）
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 时段ID |


- **请求体 `payload`**（字段可选）:


| 字段名       | 类型      | 必填  | 说明                 |
| --------- | ------- | --- | ------------------ |
| date      | string  | 否   | 日期（如 `2026-03-17`） |
| start     | string  | 否   | 开始时间               |
| end       | string  | 否   | 结束时间               |
| available | boolean | 否   | 是否可预约              |
| amount    | number  | 否   | 价格金额（>= 0）         |
| currency  | string  | 否   | 货币代码（最长 10）       |
| duration  | number  | 否   | 时长（分钟，> 0）         |
| type      | string  | 否   | 咨询类型（最长 20）       |
| detail    | string  | 否   | 补充说明（文本）          |


- **响应**: 返回更新后的时段对象。
- **响应示例**:

```json
{
  "id": "slot-9",
  "specialistId": "sp-1",
  "date": "2026-03-18",
  "start": "14:30",
  "end": "15:00",
  "available": false,
  "amount": 150.00,
  "currency": "CNY",
  "duration": 30,
  "type": "offline",
  "detail": "线下面询，地点：A 楼 302"
}
```

---

#### 5.5 删除时段

- **URL**: `/specialist/slots/{id}`
- **方法**: `DELETE`
- **请求头**: 需要 `Authorization`（专家角色）
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 时段ID |


- **请求体**: 无
- **响应**: 沿用项目统一响应风格（如成功消息或被删除对象）。
- **响应示例**:

```json
{
  "code": "OK",
  "message": "时段删除成功"
}
```

---

## 五、后台管理（Admin）

> 以下接口通常要求管理员角色，并通过 `Authorization` 校验。

### - [Done] 1. 创建专家

- **URL**: `/admin/specialists`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`
- **请求体 `payload`**（示例）:


| 字段名          | 类型       | 必填  | 说明       |
| ------------ | -------- | --- | -------- |
| name         | string   | 是   | 专家姓名     |
| userEmail    | string   | 是   | 专家账号邮箱（后端字段名为 `userEmail`） |
| password     | string   | 否   | 初始密码；不传时后端默认 `"123456"` |
| expertiseIds | string[] | 是   | 专长 ID 列表 |
| price        | number   | 否   | 默认价格     |
| bio          | string   | 否   | 简介       |

- **请求示例**:

```json
{
  "name": "李老师",
  "userEmail": "specialist@example.com",
  "password": "123456",
  "expertiseIds": ["e1"],
  "price": 50,
  "bio": "多年咨询经验..."
}
```

- **响应**: 返回创建后的专家对象（统一用 `Result` 包裹）。

```json
{
  "code": "OK",
  "message": "success",
  "data": {
    "userId": "sp-1",
    "name": "李老师",
    "status": "Active",
    "price": 50,
    "bio": "多年咨询经验..."
  }
}
```

---

### - [Done] 2. 更新专家信息

- **URL**: `/admin/specialists/{id}`
- **方法**: `PATCH`
- **请求头**: 需要 `Authorization`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 专家ID |


- **请求体 `payload`**（字段可选，局部更新）:

| 字段名          | 类型             | 必填  | 说明 |
| ------------ | ---------------- | --- | --- |
| name         | string           | 否   | 专家姓名 |
| expertiseIds | string[]         | 否   | 专长 ID 列表 |
| price        | number           | 否   | 价格 |
| bio          | string           | 否   | 简介 |
| status       | string           | 否   | `Active` / `Inactive` |

> 注意：当前后端 `EditSpecialistRequest.price` 存在默认值（50），如果前端想“不修改 price”，建议显式不传该字段或后端去掉默认值以避免误更新。

- **请求示例**（只改姓名）:

```json
{
  "name": "新名字"
}
```

- **响应**: 返回更新后的专家对象（统一用 `Result` 包裹）。

---

### - [Done] 3. 设置专家状态

- **URL**: `/admin/specialists/{id}/status`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 专家ID |


- **请求体 `payload`**（示例）:


| 字段名    | 类型     | 必填  | 说明                        |
| ------ | ------ | --- | ------------------------- |
| status | string | 是   | 如 `Active` / `Inactive` 等 |


- **响应**: 返回状态更新后的专家对象（统一用 `Result` 包裹）。

```json
{
  "code": "OK",
  "message": "success",
  "data": {
    "userId": "sp-1",
    "status": "Inactive"
  }
}
```

---

### - [Done] 4. 删除专家

- **URL**: `/admin/specialists/{id}`
- **方法**: `DELETE`
- **请求头**: 需要 `Authorization`
- **路径参数**:

| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 专家ID |

- **请求体**: 无
- **响应**: 统一用 `Result` 包裹（可返回被删除对象或成功消息，按后端实现为准）。

---

### - [Done] 3.1 管理员查询时段列表

- **URL**: `/admin/slots`
- **方法**: `GET`
- **请求头**: 需要 `Authorization`
- **查询参数 `params`**（可选）:


| 字段名          | 类型      | 说明                   |
| ------------ | ------- | -------------------- |
| specialistId | string  | 按专家 ID 筛选            |
| date         | string  | 日期筛选（如 `2026-03-17`） |
| from         | string  | 起始时间筛选（可选）           |
| to           | string  | 结束时间筛选（可选）           |
| available    | boolean | 是否仅返回可用时段（可选）        |


- **响应**: 返回时段对象列表。
- **响应示例**:

```json
[
  {
    "id": "slot-1",
    "specialistId": "sp-1",
    "date": "2026-03-17",
    "start": "09:00",
    "end": "09:30",
    "available": true,
    "amount": 120.00,
    "currency": "CNY",
    "duration": 30,
    "type": "online",
    "detail": "首次沟通，聚焦职业方向评估"
  }
]
```

---

### - [Done] 3.2 管理员创建时段

- **URL**: `/admin/slots`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`
- **请求体 `payload`**:


| 字段名          | 类型      | 必填  | 说明                       |
| ------------ | ------- | --- | ------------------------ |
| specialistId | string  | 是   | 专家 ID                    |
| date         | string  | 是   | 日期（如 `2026-03-17`）       |
| start        | string  | 是   | 开始时间（如 `09:00` 或 ISO 时间） |
| end          | string  | 是   | 结束时间（如 `09:30` 或 ISO 时间） |
| available    | boolean | 否   | 是否可预约，默认 `true`          |
| amount       | number  | 否   | 价格金额，默认 `0.00`           |
| currency     | string  | 否   | 货币代码，默认 `CNY`            |
| duration     | number  | 是   | 时长（分钟），必须大于 0            |
| type         | string  | 否   | 咨询类型，默认 `online`         |
| detail       | string  | 否   | 补充说明（文本）                 |


- **响应**: 返回创建后的时段对象。
- **响应示例**:

```json
{
  "id": "slot-9",
  "specialistId": "sp-1",
  "date": "2026-03-18",
  "start": "14:00",
  "end": "14:30",
  "available": true,
  "amount": 120.00,
  "currency": "CNY",
  "duration": 30,
  "type": "online",
  "detail": "首次沟通，聚焦职业方向评估"
}
```

---

### - [Done] 3.3 管理员更新时段

- **URL**: `/admin/slots/{id}`
- **方法**: `PATCH`
- **请求头**: 需要 `Authorization`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 时段ID |


- **请求体 `payload`**（字段可选）:


| 字段名       | 类型      | 必填  | 说明                 |
| --------- | ------- | --- | ------------------ |
| date      | string  | 否   | 日期（如 `2026-03-17`） |
| start     | string  | 否   | 开始时间               |
| end       | string  | 否   | 结束时间               |
| available | boolean | 否   | 是否可预约              |
| amount    | number  | 否   | 价格金额（>= 0）         |
| currency  | string  | 否   | 货币代码（最长 10）       |
| duration  | number  | 否   | 时长（分钟，> 0）         |
| type      | string  | 否   | 咨询类型（最长 20）       |
| detail    | string  | 否   | 补充说明（文本）          |


- **响应**: 返回更新后的时段对象。
- **响应示例**:

```json
{
  "id": "slot-9",
  "specialistId": "sp-1",
  "date": "2026-03-18",
  "start": "14:30",
  "end": "15:00",
  "available": false,
  "amount": 150.00,
  "currency": "CNY",
  "duration": 30,
  "type": "offline",
  "detail": "线下面询，地点：A 楼 302"
}
```

---

### - [Done] 3.4 管理员删除时段

- **URL**: `/admin/slots/{id}`
- **方法**: `DELETE`
- **请求头**: 需要 `Authorization`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 时段ID |


- **请求体**: 无
- **响应**: 沿用项目统一响应风格（如成功消息或被删除对象）。
- **响应示例**:

```json
{
  "code": "OK",
  "message": "时段删除成功"
}
```

---

### - [Done] 3.5 管理员查看预约列表

- **URL**: `/admin/bookings`
- **方法**: `GET`
- **请求头**: 需要 `Authorization`
- **查询参数 `params`**（可选）:


| 字段名      | 类型     | 说明   |
| -------- | ------ | ---- |
| page     | number | 页码   |
| pageSize | number | 每页条数 |


- **响应**: 返回分页后的预约列表对象。
- **响应示例**:

```json
{
  "items": [
    {
      "id": "bk-101",
      "status": "Pending",
      "time": "2026-03-17T09:00:00+08:00",
      "customerId": "user-1",
      "customerName": "张三",
      "specialistId": "sp-1",
      "specialistName": "李老师",
      "note": "希望聊职业规划",
      "price": 300,
      "createdAt": "2026-03-10T08:30:00+08:00",
      "updatedAt": "2026-03-10T08:30:00+08:00"
    }
  ],
  "total": 1,
  "page": 1,
  "pageSize": 10
}
```

---

### - [Done] 3.6 管理员查看预约详情

- **URL**: `/admin/bookings/{id}`
- **方法**: `GET`
- **请求头**: 需要 `Authorization`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 预约ID |


- **响应**: 返回单个预约详情对象。
- **响应示例**:

```json
{
  "id": "bk-101",
  "status": "Pending",
  "time": "2026-03-17T09:00:00+08:00",
  "customerId": "user-1",
  "customerName": "张三",
  "specialistId": "sp-1",
  "specialistName": "李老师",
  "note": "希望聊职业规划",
  "price": 300,
  "createdAt": "2026-03-10T08:30:00+08:00",
  "updatedAt": "2026-03-10T08:30:00+08:00"
}
```

---

### - [Done] 4. 创建专长

- **URL**: `/admin/expertise`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`
- **请求体 `payload`**:


| 字段名         | 类型     | 必填  | 说明   |
| ----------- | ------ | --- | ---- |
| name        | string | 是   | 专长名称 |
| description | string | 否   | 描述   |


- **响应**: 返回创建后的专长对象。

---

### - [Done] 5. 更新专长

- **URL**: `/admin/expertise/{id}`
- **方法**: `PATCH`
- **请求头**: 需要 `Authorization` 
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 专长ID |


- **请求体 `payload`**: 与创建类似，字段可选。
- **响应**: 返回更新后的专长对象。

---

### - [Done] 6. 删除专长

- **URL**: `/admin/expertise/{id}`
- **方法**: `DELETE`
- **请求头**: 需要 `Authorization`
- **路径参数**:


| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | 专长ID |


- **请求体**: 无
- **响应**: 沿用项目统一响应风格（如成功消息或被删除对象）。
- **响应示例**:

```json
{
  "code": "OK",
  "message": "专长删除成功"
}
```

---

## 六、定价（Pricing）

### - [Done] 1. 获取价格报价

- **URL**: `/pricing/quote`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`（视业务而定，如仅登录用户可报价）
- **请求体 `payload`**（示例）:


| 字段名          | 类型     | 必填  | 说明            |
| ------------ | ------ | --- | ------------- |
| specialistId | string | 是   | 专家 ID         |
| duration     | number | 否   | 时长（分钟）        |
| type         | string | 否   | 服务类型，如 online |


- **响应示例**:

```json
{
  "amount": 300,
  "currency": "CNY",
  "detail": "60 分钟在线咨询"
}
```

### - [Done] 2. Admin 查询 Pricing Rules

- **URL**: `/admin/pricing-rules`
- **方法**: `GET`
- **请求头**: 需要 `Authorization`

| 字段名       | 类型   | 必填 | 说明 |
| ---------- | ---- | -- | -- |
| specialistId | string | 否 | 按专家 ID 过滤 |
| duration   | number | 否 | 按时长（分钟）过滤 |
| type       | string | 否 | 按服务类型过滤 |

- **响应示例**:

```json
{
  "code": "OK",
  "message": "success",
  "data": [
    {
      "id": "rule-1",
      "specialistId": "u3",
      "duration": 60,
      "type": "online",
      "amount": 100.0,
      "currency": "USD",
      "detail": "1 hour online session",
      "createdAt": "2026-04-17T14:30:00",
      "updatedAt": "2026-04-17T14:30:00"
    }
  ]
}
```

### - [Done] 3. Admin 查询单个 Pricing Rule

- **URL**: `/admin/pricing-rules/{id}`
- **方法**: `GET`
- **请求头**: 需要 `Authorization`

- **响应示例**:

```json
{
  "code": "OK",
  "message": "success",
  "data": {
    "id": "rule-1",
    "specialistId": "u3",
    "duration": 60,
    "type": "online",
    "amount": 100.0,
    "currency": "USD",
    "detail": "1 hour online session",
    "createdAt": "2026-04-17T14:30:00",
    "updatedAt": "2026-04-17T14:30:00"
  }
}
```

### - [Done] 4. Admin 新增 Pricing Rule

- **URL**: `/admin/pricing-rules`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`

| 字段名       | 类型   | 必填 | 说明 |
| ---------- | ---- | -- | -- |
| specialistId | string | 是 | 专家 ID |
| duration   | number | 是 | 时长（分钟） |
| type       | string | 是 | 服务类型 |
| amount     | number | 是 | 价格 |
| currency   | string | 否 | 货币，默认 `CNY` |
| detail     | string | 否 | 规则说明 |

- **Request Body**:

```json
{
  "specialistId": "u3",
  "duration": 90,
  "type": "offline",
  "amount": 168.0,
  "currency": "CNY",
  "detail": "90 minute office session"
}
```

- **响应示例**:

```json
{
  "code": "OK",
  "message": "success",
  "data": {
    "id": "rule-2",
    "specialistId": "u3",
    "duration": 90,
    "type": "offline",
    "amount": 168.0,
    "currency": "CNY",
    "detail": "90 minute office session"
  }
}
```

### - [Done] 5. Admin 编辑 Pricing Rule

- **URL**: `/admin/pricing-rules/{id}`
- **方法**: `PATCH`
- **请求头**: 需要 `Authorization`
- **Request Body**: 与新增 Pricing Rule 一致

- **响应示例**:

```json
{
  "code": "OK",
  "message": "success",
  "data": {
    "id": "rule-2",
    "specialistId": "u3",
    "duration": 90,
    "type": "offline",
    "amount": 188.0,
    "currency": "USD",
    "detail": "Updated session"
  }
}
```

### - [Done] 6. Admin 删除 Pricing Rule

- **URL**: `/admin/pricing-rules/{id}`
- **方法**: `DELETE`
- **请求头**: 需要 `Authorization`

- **响应示例**:

```json
{
  "code": "OK",
  "message": "pricing rule deleted successfully"
}
```

---

## 七、支付相关（Booking 扩展功能）

### - [ ] 1. 支付预约（创建支付单 + 确认支付）

> 当前流程：客户先创建支付单并完成支付，再调用 `/bookings` 创建预约；如点击 **Pay Later**，可通过 unpaid 接口继续支付；如点击 **Cancel**，可取消该支付意图并从未支付列表移除。

#### 7.1 创建支付单（获取二维码）

- **URL（推荐）**: `/bookings/{id}/payment`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`
- **路径参数**:

| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | `paymentIntentId`（前端生成，建议 UUID） |

- **请求体 `payload`**（建议）:

| 字段名          | 类型     | 必填  | 说明 |
| ------------ | ------ | --- | --- |
| specialistId | string | 否   | 专家 ID（便于后端校验/记录） |
| slotId       | string | 否   | 时段 ID（便于后端校验/记录） |
| amount       | number | 否   | 支付金额（后端可忽略并以服务端计算为准） |
| currency     | string | 否   | 货币（默认 `CNY`） |
| scene        | string | 否   | 场景标记，建议传 `booking` |

- **响应示例**:

```json
{
  "paymentId": "BK1745116841234abc123",
  "paymentToken": "BK1745116841234abc123",
  "qrCodeUrl": "https://api.qrserver.com/v1/create-qr-code/?size=280x280&data=...",
  "amount": 120.00,
  "currency": "CNY"
}
```

#### 7.2 确认支付成功

- **URL（推荐）**: `/bookings/{id}/payment/confirm`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`
- **路径参数**:

| 字段名 | 类型     | 说明   |
| --- | ------ | ---- |
| id  | string | `paymentIntentId`（与 7.1 相同） |

- **请求体 `payload`**（建议）:

| 字段名      | 类型     | 必填  | 说明 |
| -------- | ------ | --- | --- |
| paymentId | string | 否   | 支付单 ID |
| status   | string | 否   | 前端透传字段；后端以支付宝查询结果为准 |

- **响应示例**:

```json
{
  "id": "pi_xxx",
  "paymentId": "BK1745116841234abc123",
  "paymentStatus": "SUCCESS",
  "status": "Pending"
}
```

#### 7.3 支付宝异步回调（notify）

- **URL**: `/bookings/alipay/notify`
- **方法**: `POST`
- **认证**: 无（支付宝服务器回调）
- **请求参数**: 支付宝回调标准表单参数（如 `out_trade_no`、`trade_status`、`sign` 等）
- **响应**:
  - 验签并处理成功：返回纯文本 `success`
  - 验签失败或处理失败：返回纯文本 `failure`

- **实现说明（当前后端）**:
  - 回调会先做支付宝签名校验（RSA2）
  - 当 `trade_status` 为 `TRADE_SUCCESS` 或 `TRADE_FINISHED` 时记为已支付

#### 7.4 获取未支付订单列表（悬浮球）

- **URL**: `/bookings/unpaid-payments`
- **方法**: `GET`
- **请求头**: 需要 `Authorization`（顾客）
- **说明**: 返回当前用户 Redis 中仍有效的未支付订单（含剩余有效时间）

- **响应示例**:

```json
{
  "items": [
    {
      "paymentIntentId": "pi_xxx",
      "paymentId": "BK1745116841234abc123",
      "specialistId": "u3",
      "slotId": "slot-1",
      "slotLabel": "2026-04-22 09:30 - 10:30",
      "amount": 120.00,
      "currency": "CNY",
      "status": "UNPAID",
      "expiresAt": "2026-04-21T18:30:00+08:00",
      "remainingSeconds": 420
    }
  ]
}
```

#### 7.5 获取单个未支付订单详情（前端无对应引用，为以后扩展功能而预留）

- **URL**: `/bookings/unpaid-payments/{id}`
- **方法**: `GET`
- **请求头**: 需要 `Authorization`（顾客）
- **路径参数**:

| 字段名 | 类型     | 说明 |
| --- | ------ | --- |
| id  | string | `paymentIntentId` |

- **响应**: 返回单个 `UnpaidPaymentItemResult`，字段同 7.4 的 `items` 元素。

#### 7.6 继续支付（刷新二维码）

- **URL**: `/bookings/unpaid-payments/{id}/resume`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`（顾客）
- **路径参数**:

| 字段名 | 类型     | 说明 |
| --- | ------ | --- |
| id  | string | `paymentIntentId` |

- **说明**: 重新发起支付宝预下单并返回新的二维码（旧支付单会失效）。
- **响应示例**:

```json
{
  "paymentId": "BK1745116999999abc123",
  "paymentToken": "BK1745116999999abc123",
  "qrCodeUrl": "https://api.qrserver.com/v1/create-qr-code/?size=280x280&data=...",
  "amount": 120.00,
  "currency": "CNY"
}
```

#### 7.7 模拟支付成功（测试环境）

- **URL**: `/bookings/{id}/payment/mock-success`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`（顾客）
- **路径参数**:

| 字段名 | 类型     | 说明 |
| --- | ------ | --- |
| id  | string | `paymentIntentId` |

- **说明**:
  - 仅测试用途；由后端配置项 `payment.mock-enabled` 控制是否可用
  - 成功后会将该 intent 在 Redis 草稿中标记为已支付（等价于支付成功）
- **请求体**: 无
- **响应示例**:

```json
{
  "id": "pi_xxx",
  "paymentId": "BK1745116999999abc123",
  "paymentStatus": "SUCCESS",
  "status": "Pending"
}
```

#### 7.8 取消未支付订单（Cancel）

- **URL**: `/bookings/unpaid-payments/{id}/cancel`
- **方法**: `POST`
- **请求头**: 需要 `Authorization`（顾客）
- **路径参数**:

| 字段名 | 类型     | 说明 |
| --- | ------ | --- |
| id  | string | `paymentIntentId` |

- **说明**:
  - 仅可取消当前用户自己的未支付 intent
  - 取消后会清理 Redis 支付草稿、`outTradeNo` 映射和 unpaid 索引
  - 已支付、已过期或无权限时返回明确错误
- **请求体**: 无
- **响应示例**:

```json
{
  "code": "OK",
  "message": "支付单已取消",
  "data": null
}
```
